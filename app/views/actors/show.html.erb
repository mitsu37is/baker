<div class="text-black font-bold text-xl w-5/6 m-auto my-4 text-center"><%= @actor.name %>さん 登場キャラクター</div>
<div class="flex flex-wrap justify-start">
  <% @actor.characters.each do |character| %>
    <div class="max-w-sm m-4 lg:flex">
      <div class="border border-grey-light bg-white rounded p-4 leading-normal">
        <div class="mb-8">
          <div class="text-black font-bold text-md mb-2"><%= link_to character.name, character_path(character) %></div>
          <p class="text-grey-darker text-base"><%= character.anime[0].title %></p>
        </div>
      </div>
    </div>
  <% end %>
</div>